# Uncomment the next line to define a global platform for your project
platform :ios, '8.0'
use_frameworks!
target 'HttpManager' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HttpManager
pod 'SwiftSocket'
  target 'HttpManagerTests' do
    inherit! :search_paths
    # Pods for testing
    
  end

  target 'HttpManagerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
